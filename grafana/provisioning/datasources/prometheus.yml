# Grafana datasource provisioning — Prometheus
apiVersion: 1

datasources:
  - name: Prometheus
    uid: apix-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      httpMethod: POST
    editable: true